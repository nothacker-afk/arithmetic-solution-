apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "arithmetic.fullname" . }}-config
  labels:
    {{- include "arithmetic.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
  DB_PATH: "/data/arithmetic.db"
